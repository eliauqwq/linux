>>>>>>>>数据结构与算法之kmp算法
    KMP（Knuth-Morris-Pratt）算法是一种用于字符串匹配的经典算法，它由 Donald Knuth、James H. Morris 和 Vaughan
Pratt 于1977年提出，因三人名字的首字母得名。
    KMP的核心思想在于避免在匹配失败时，从头开始重新匹配。我们知道字符串传统暴力匹配算法在母串匹配时如果匹配失败，
将会从子串的开头重新匹配。而KMP算法则旨在避免重新开始，想办法避免前面重复匹配的过程。其实现离不开next数组

一、什么是next数组
1.前后缀
对于一个字符串，我们这样定义它的前后缀
#前缀：指包含第一个字符的所有子串
#后缀：指包含最后一个字符的所有子串

next数组所包含的信息就是，从第一个字符到当前位置的子串拥有的最大相等前后缀。；例如对于一个待匹配的串"ababc",当匹配到'c'时，如果发现匹配失败，那么之前的"abab"最大相等前后缀长度是2，那么我们就可以直接跳过前两个字符，从第三个开始匹配。

2.求next数组





